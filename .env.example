# Copy to .env for `docker compose`. All optional — sensible defaults apply.
EA_TOKEN=changeme
# AI Review / auto-tag. Leave blank to disable (endpoints degrade gracefully).
ANTHROPIC_API_KEY=
AI_MODEL=claude-opus-4-8
AI_MODEL_FALLBACK=claude-sonnet-5
